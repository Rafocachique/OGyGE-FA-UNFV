rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Allow authenticated users to read and write to the thesis-plans folder
    match /thesis-plans/{planId}/{allPaths=**} {
      allow read, write: if request.auth != null;
    }
  }
}
